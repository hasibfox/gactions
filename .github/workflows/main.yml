name: Hello-world
 
on: [push]
 
jobs:
    hello-world:
        runs-on: ubuntu-latest
        steps:
        # checkout the repo
        - name: 'Checkout GitHub Action'
        - run: |
            echo "hello work"
